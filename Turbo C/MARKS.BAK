#include<stdio.h>
#include<conio.h>
void main()
{
	int english,gujarati,hindi,total;
	float per;
	clrscr();
	printf("enter the marks of english, gujarati & hindi:\n");
	scanf("%d%d%d",&english,&gujarati,&hindi);
	total=english+gujarati+hindi;
	printf("\ntotal is:%d",total);
	per=total/3;
	printf("\npersentage is:%.2f",per);
	if(per>=75)
	{
		printf("\nresult is grade A");
	}
	else if(per>=60 && per<75)
	{
		printf("\nresult is grade B");
	}
	else if(per>=45 && per<60)
	{
		printf("\nresult is grade C");
	}
	else if(per>=35 && per<45)
	{
		printf("\nresult is grade D");
	}
	else if(per<=35)
	{
		printf("\nresult is Fail");
	}
	getch();
}