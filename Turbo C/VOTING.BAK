#include<stdio.h>
#include<conio.h>
void main()
{
	int age;
	clrscr();
	printf("enter the person age:");
	scanf("%d",&age);
	if(age>=18)
	{
		printf("age %d is eligible for voting");
	}
	else
	{
		printf("age %d is not eligible for voting");
	}
	getch();
}
